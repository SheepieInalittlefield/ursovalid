%A receiveRequest(P, leave ) followed by a assign(taxiway, P ) for a single
%plane P, shall eventually be followed by a assign(gate1, P ) or assign(gate2,
%P )
forall P: Plane .
    [true* . receiveRequest(P, leave) . !assign(taxiway, P)* . assign(taxiway, P)]<true* . assign(runway, P)>true