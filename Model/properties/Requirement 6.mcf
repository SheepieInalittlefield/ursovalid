%A receiveRequest(P, land ) followed by a assign(runway, P ) for a single
%plane P, shall eventually be followed by a assign(gate1, P ) or assign(gate2,
%P )
forall P: Plane .
    [true*.receiveRequest(P, land). !assign(runway, P)* .assign(runway, P)]
        mu X . 
            ( <true>true && [ !(assign(gate1, P) || assign(gate2, P) ) ]X )